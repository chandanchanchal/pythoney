#
data = [
    (1, "Alice", "IT", 70000),
    (2, "Bob", "HR", 50000),
    (3, "Charlie", "IT", 80000)
]

columns = ["emp_id", "name", "dept", "salary"]

df = spark.createDataFrame(data, columns)
df.show()

df.write \
  .format("delta") \
  .mode("overwrite") \
  .save("/tmp/employee_delta")


